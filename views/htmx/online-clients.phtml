<?php

declare(strict_types=1);

use TeamspeakServerManager\DTO\Client;

/** @var $vars array<mixed> */

/** @var array<Client> $clients */
$clients = $vars['clients'];

?>
<?php if ($clients === []): ?>
    <article hx-get="/" hx-trigger="every 5s" hx-swap="outerHTML">
        <p class="text-center mb-0">
            No one is currently online
        </p>
    </article>
<?php else: ?>
    <article hx-get="/" hx-trigger="every 5s" hx-swap="outerHTML">
        <figure class="mb-0" >
            <table role="grid" class="mb-0">
                <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Name</th>
                    <th scope="col">Connected</th>
                    <th scope="col">Online</th>
                    <th scope="col">Idle</th>
                </tr>
                </thead>
                <tbody>
                <?php $i = 1 ?>
                <?php foreach ($clients as $client): ?>
                    <tr>
                        <th scope="row"><?= $i ?></th>
                        <td><?= escape($client->nickname) ?></td>
                        <td><?= $client->getLastConnectedReadable() ?></td>
                        <td><?= $client->getOnlineReadable() ?></td>
                        <td><?= $client->getIdleTimeReadable() ?></td>
                    </tr>
                    <?php $i = $i + 1 ?>
                <?php endforeach; ?>
                </tbody>
            </table>
        </figure>
    </article>
<?php endif; ?>
